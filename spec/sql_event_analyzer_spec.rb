require 'spec_helper'

describe SqlEventAnalyzer do
  it 'has a version number' do
    expect(SqlEventAnalyzer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
